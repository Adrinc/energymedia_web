/* 
import { translations } from '../../data/translations';
import { isEnglish } from '../../data/variables'; */

const FootUWIFI = () => {
/*         const ingles = useStore(isEnglish);
        const t = ingles ? translations.en : translations.es; */
  <footer className="flex flex-col md:flex-row justify-between items-center p-4 bg-gray-800 text-white h-8">
    <div className="flex h-80 w-full md:w-1/3">
      <div className="flex flex-row justify-center p-4 items-center gap-1 w-full md:w-auto">
        <a href="#top" className="title">Go Top</a>
      </div>
      <p className="infotext">Copyright © 2024 CBLUNA</p>
    </div>
  {/*   <style jsx>{`
      .columnaDerecha {
        display: flex;
        flex-direction: column;
        height: 100%;
        gap: 24px;
        justify-content: center;
        align-items: flex-end;
        align-content: flex-end;
        width: 100%;
        order: 3;
      }
      .title {
        font-size: clamp(18px, 2vw, 32px);
        text-align: center;
        color: var(--primary-text-color);
      }
      .infotext {
        font-size: clamp(16px, 3vw, 18px);
        color: var(--primary-text-color);
        font-weight: 400;
        font-family: var(--font-family-secondary);
        text-align: center;
      }
      .image {
        width: clamp(50px, 5vw, 85px);
        height: clamp(50px, 5vw, 85px);
        filter: invert(1);
      }
      .top-icon {
        width: clamp(25px, 5vw, 55px);
        height: clamp(25px, 5vw, 55px);
        filter: invert(1);
      }
      @media (max-width: 768px) {
        footer {
          flex-direction: column;
          height: 100%;
          gap: 8px;
        }
        .columnaDerecha {
          width: 100%;
          justify-content: center;
          align-items: center;
          align-content: center;
          padding: 8px;
          order: 3;
        }
        .fila-iconos {
          display: flex;
          flex-direction: row;
          justify-content: space-evenly;
          align-items: center;
          width: 100%;
        }
      }
    `}</style> */}
  </footer>
 };

export default FootUWIFI;
