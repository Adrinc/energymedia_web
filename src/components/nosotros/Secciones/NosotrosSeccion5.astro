---
import RiveComponent from "../../global/animations/riveComponent";
import BotonStyle1 from "../../global/BotonSt1.astro";
---


<section id="our_services">
      <div class="esfera"/> 
      <div class="gradient-background-top"></div>
      <div class="gradient-background-bottom"></div>
      <div class="rivecomp web">
        <RiveComponent
          src="/rive/cbluna-nosotros.riv"
          artboard="socio_seccion"
          stateMachines="State Machine 1"
          autoplay={true}
          fit="contain"
          client:only="react" 
          transition:persist
        />
      </div>
      <div class="rivecomp movil">
        <RiveComponent
          src="/rive/cbluna-nosotros.riv"
          artboard="socio_seccion_mobile"
          stateMachines="State Machine 1"
          autoplay={true}
          fit="contain"
          client:only="react" 
          transition:persist
        />


<!-- 
      <div class="esfera"/> -->
      <!-- <div class="boton">
        <BotonStyle1
          link="/proyectos"
          texto={"Contacto"}
          fontsize="48px"
          fontweight="bold"
          showIcon={false}
        />
      </div> -->
    </section>


<style> 

section {
      position: relative;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      width: 100vw;
      height: 65vh;
      overflow: hidden;
      }
      .gradient-background-top {
      position: absolute;
      top: 0;
      width: 100vw;
      height: 7vh;
      background: linear-gradient(to bottom, #375beb4a -100%, transparent 100%);
      z-index: 1;
      }

    .gradient-background-bottom {
      position: absolute;
      bottom: 0;
      width: 100vw;
      height: 7vh;
      background: linear-gradient(to top, #375beb4a -100%, transparent 100%);
      z-index: 1;
      }
      .rivecomp {
      position: absolute;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      width: 100%;
      height: 100%;
      margin-top: 5vh;
      margin-bottom: 5vh;
      z-index: 2;
      }
      
      .boton {
    position: absolute;
    top: 50%;
    left: 80%;
    transform: translate(-50%, -50%);
    z-index: 2;
  }
.esfera {
  position: absolute;
  top: 50%;
    left: 82%; /* Mueve la esfera a la derecha */
  transform: translate(-50%, -50%);
  
  width: 1000px;
  height: 1000px;
  /* gradiente de color 375BEB a transparente radial centrado */
  background: radial-gradient(circle at center, #375beb4a 10%, #141414 30%, transparent 100%);
  z-index: -1;
}

.web {
  display: block;
 
}

.movil {
  display: none;

}

@media (max-width: 900px) {
  section {
    cursor: default;
    height: 1080px;
  }
  .esfera {
  position: absolute;
  top: 85%;
    left: 50%; /* Mueve la esfera a la derecha */
  transform: translate(-50%, -50%);
  
  width: 650px;
  height: 650px;
  /* gradiente de color 375BEB a transparente radial centrado */
  background: radial-gradient(circle at center, #375beb4a 10%, #141414 30%, transparent 100%);
  z-index: -1;
}
  .web {
    display: none;
  }
  .movil {
    display: block;
  }
}

</style>