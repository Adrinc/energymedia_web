---
---

<section id="stats_section" class="parallax">
  <div class="gradient-background-top"></div>
  <div class="gradient-background-bottom"></div>

  <div class="stats-container">
    <div class="stats-left">
      <div class="image-container">
        <img src="/image/global/71.jpg" alt="Personas trabajando" class="stats-image" />
        <div class="experience-tag">
          <span>20 AÑOS DE EXPERIENCIA</span>
        </div>
      </div>
    </div>

    <div class="stats-right">
      <h1>
        <span class="stats-title">ESTADÍSTICAS</span>
        <br /> IMPULSANDO <span class="gradient-text">RESULTADOS</span> QUE MARCAN LA DIFERENCIA
      </h1>
      <p>
        Cada proyecto que realizamos refleja nuestro compromiso con la innovación y la excelencia.
        Estas cifras demuestran el impacto de nuestro trabajo en el crecimiento y éxito de nuestros clientes.
      </p>

      <div class="progress-bars">
        <div class="progress-item">
          <div class="progress-label">
            <span>Clientes satisfechos con las soluciones entregadas:</span>
            <span class="progress-percentage">96%</span>
          </div>
          <div class="progress-bar">
            <div class="progress-filled" style="width: 96%;"></div>
          </div>
        </div>

        <div class="progress-item">
          <div class="progress-label">
            <span>Retención de clientes gracias a nuestro servicio de calidad:</span>
            <span class="progress-percentage">95%</span>
          </div>
          <div class="progress-bar">
            <div class="progress-filled" style="width: 95%;"></div>
          </div>
        </div>

        <div class="progress-item">
          <div class="progress-label">
            <span>Cumplimiento en tiempos de entrega y objetivos planteados:</span>
            <span class="progress-percentage">90%</span>
          </div>
          <div class="progress-bar">
            <div class="progress-filled" style="width: 90%;"></div>
          </div>
        </div>

        <div class="progress-item">
          <div class="progress-label">
            <span>Clientes que reportan un aumento de ingresos después de implementar nuestras soluciones:</span>
            <span class="progress-percentage">92%</span>
          </div>
          <div class="progress-bar">
            <div class="progress-filled" style="width: 92%;"></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Estilo general para la sección */
  section {
    position: relative;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    width: 100vw;
    min-height: 80vh;
    padding: 5vw;
    overflow: hidden;
    background-image: url('/image/backgrounds/bg2t.png');
    background-position: center;
    background-repeat: no-repeat;
    background-size: 100% 100%;
    
    color: #fff;
  }

  /* Efecto Parallax */
  .parallax {
    background-attachment: fixed;
    background-size: cover;
  }
  /* Gradientes superior e inferior */
  .gradient-background-top,
  .gradient-background-bottom {
    position: absolute;
    width: 100vw;
    height: 7vh;
    z-index: 1;
  }
  
  .gradient-background-top {
  top: 0;
  background: linear-gradient(to bottom, #375beb4a -100%, transparent 100%);
}

.gradient-background-bottom {
  bottom: 0;
  background: linear-gradient(to top, #375beb4a -100%, transparent 100%);
}
  
.stats-container {
    position: relative;
    z-index: 1;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
    gap: 40px;
    padding: 40px;
    z-index: 2;
    box-shadow:     0 8px 20px #67c7ffcc, 0 -8px 20px #ab39ff66,0 8px 20px #5c00ff4d;    
    backdrop-filter: blur(50px);
    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
    border-radius: 20px;
  }



  
  @media (max-width: 900px) {
    .stats-container {
      flex-direction: column;
    }
  }
  
  .gradient-text {
    background: linear-gradient(90deg, #67c7ff, #ad39ff, #5d00ff);
    -webkit-background-clip: text;
    color: transparent;
  }
  
  /* Sección izquierda */
  .stats-left {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .image-container {
    position: relative;
    border-radius: 20px 40% 20px 20px;
 
    background: linear-gradient(135deg, #8c09ff, #008bff);
    padding: 8px;
    width: 100%;
    max-width: 500px;
    height: auto;

    transition: transform 0.3s ease;
  }
  
  .stats-image {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 3 / 4; 
    object-fit: cover;
    border-radius: 12px 40% 12px 12px;
    background-color: aqua;
  }
  
  .image-container:hover {
    transform: scale(1.05);
  }
  
  .experience-tag {
    position: absolute;
    display: flex;
    align-items: center;
    justify-content: center;
    bottom: -10px;
    left: -20px;
    background: linear-gradient(90deg, #8c09ff, #375beb);
    color: #fff;
    padding: 15px 30px;
    font-weight: bold;
    font-size: 1.2rem;
    border-radius: 12px;
    text-align: center;
    height: 100px;
    width: 200px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
    border: 5px solid linear-gradient(90deg, #8c09ff, #375beb);;
  }
  
  /* Sección derecha */
  .stats-right {
    flex: 2;
    display: flex;
    flex-direction: column;
    gap: 30px;
  }
  
  body {
    font-family: 'Poppins', sans-serif; /* Fuente moderna */
  }
  
  .stats-title {
    text-decoration: underline;
    text-underline-offset: 8px;
  }
  
  .stats-right h1 {
    font-size: clamp(2.5rem, 6vw, 4rem); /* Título más impactante */
    color: #fff;
    margin: 0;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 2px;
  }
  
  .stats-right p {
    font-size: clamp(1rem, 2vw, 1.8rem);
    color: #bbb;
    line-height: 1.8;
    margin: 20px 0;
  }
  
  /* Barras de progreso */
  .progress-bars {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }
  
  .progress-item {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  
  .progress-label {
    display: flex;
    justify-content: space-between;
    font-size: clamp(1rem, 2vw, 1.6rem);
    color: #fff;
  }
  
  .progress-label span:first-child {
    display: flex;
    align-items: center;
  }
  
  .progress-label span:first-child:before {
    content: "✔"; /* Ejemplo de icono */
    margin-right: 8px;
    color: #0015ff;
  }
  
  .progress-bar {
    width: 100%;
    background-color: #202020;
    border-radius: 8px;
    overflow: hidden;
    height: 12px;
    box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.5);
  }
  
  .progress-filled {
    height: 100%;
    background: linear-gradient(90deg, #008bff, #8c09ff);
    transition: width 1s ease-in-out; /* Animación más suave */
    box-shadow: 0 3px 6px rgba(0, 0, 0, 0.3);
  }
</style>
