---
import RiveComponent from "../../global/animations/riveComponent";
---

<section id="servicios1">
  <!-- Gradientes de fondo -->
  <div class="gradient-background gradient-top"></div>
  <div class="gradient-background gradient-bottom"></div>

  <!-- Contenedor principal (flex) -->
  <div class="row-container">
    <!-- Columna de texto -->
    <div class="col text-column">
      <h2>🌟 Sube a la cima</h2>
      <h3>🚀 Incrementa tus ventas</h3>
      <p>
        Con un <strong>potente sistema de información</strong> podrás controlar cada detalle de tu negocio, llevando tus resultados al siguiente nivel.
      </p>

      <h3>🤝 Cuenta con nosotros</h3>
      <p><strong>Somos expertos digitales</strong></p>
      <p>
        Con años de experiencia en el desarrollo de aplicaciones web y móviles, siempre tenemos la solución perfecta para tus necesidades.
      </p>

      <h3>✅ Confianza garantizada</h3>
      <p><strong>Las mejores prácticas</strong></p>
      <p>
        Nos mantenemos actualizados con las últimas tecnologías, asegurando soluciones optimizadas, seguras y eficaces.
      </p>
    </div>

    <!-- Columna de animación -->
    <div class="col rivecomp">
      <RiveComponent
        src="/rive/macbook_animation.riv"
        artboard="macbook_animation"
        stateMachines="State Machine 1"
        autoplay={true}
        fit="contain"
        client:only="react"
        transition:persist
      />
    </div>
  </div>
</section>

<style>
section {
  position: relative;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 100vw;
  padding: 0 10vw;
  overflow: hidden;
  color: #ffffff;
  background: #0a0a0a;
}

.gradient-background {
  position: absolute;
  width: 100vw;
  height: 7vh;
  z-index: 1;
}

.gradient-top {
  top: 0;
  background: linear-gradient(to bottom, #375beb4a, transparent);
}

.gradient-bottom {
  bottom: 0;
  background: linear-gradient(to top, #375beb4a, transparent);
}

.row-container {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
  position: relative;
  z-index: 2;
  padding: 1rem;
  width: 100%;
}

.col {
  flex: 1;
  margin: 1rem;
}

.text-column {
  min-width: 50%;
  max-width: 600px;
}

h2, h3 {
  background: linear-gradient(90deg, #4a90e2, #8e44ad);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.rivecomp {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 50%;
  height: 50vw;
  max-width: 700px;
  max-height: 700px;
}

@media (max-width: 1100px) {
  .row-container {
    flex-direction: column-reverse;
  }

  .rivecomp {
    width: 100%;
    height: 100%;
    max-width: 80vw;
    max-height: 550px;
    min-height: 400px;
  }
}
</style>
